# Abot Manual

## 화면 구조

- `Mac`: `mini1`, `mini2` 중 접속할 Mac을 선택합니다.
- `Work`: 현재 Mac에서 열려 있는 tmux 작업을 선택합니다.
- `Fn`: 터미널 조작 버튼입니다.
- 아래 입력창: 한글, 음성 입력, 긴 문장을 먼저 작성하는 곳입니다.

## 입력

- `Paste`: 입력창 내용을 터미널에 붙여넣기만 합니다. 엔터는 보내지 않습니다.
- `Send`: 입력창 내용을 붙여넣고 엔터까지 보냅니다.
- `Shift+Enter`: `Send`와 같습니다.
- `Clear`: 아래 입력창만 비웁니다.

## 기능 버튼

- `Input`: 아래 입력창으로 포커스를 이동합니다.
- `Stop`: 현재 실행 중인 명령에 `Ctrl-C`를 보냅니다.
- `Esc`: Escape 키를 보냅니다.
- `C-U`: 현재 입력 중인 줄을 지웁니다.
- `Text`: 현재 tmux 화면을 복사용 텍스트 패널로 엽니다. 누르면 버튼 색이 바뀌고 흰색 텍스트 화면이 열립니다.
- `Up`, `Down`: 터미널 화면 기록을 위아래로 이동합니다.
- `Reload`: 터미널 화면을 새로고침합니다.
- `Bg`: 웹 터미널 접속만 분리합니다.
- `Help`: 화면 안 매뉴얼을 엽니다.

## 복사와 스크롤

- 오른쪽 세로 스크롤 영역을 손으로 밀면 터미널 기록을 스크롤합니다.
- 복사할 내용이 있으면 원하는 위치까지 스크롤한 뒤 `Text`를 누릅니다.
- `Text` 화면에서 필요한 부분을 드래그하거나 `Copy`로 전체 복사합니다.
